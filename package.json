{
  "engines": {
    "node": ">=20.6.0"
  },
  "name": "freshusb",
  "version": "1.0.0",
  "description": "Freshen up your USBs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file=.env --watch -r ts-node/register src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "USB",
    "format",
    "files",
    "partition"
  ],
  "author": "Ozzy",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@types/usb": "^2.0.5",
    "usb": "^2.14.0"
  }
}
